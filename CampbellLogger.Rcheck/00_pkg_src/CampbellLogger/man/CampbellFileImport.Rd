% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CampbellFileImport.R
\name{CampbellFileImport}
\alias{CampbellFileImport}
\title{import a Campbell logger *.dat file}
\usage{
CampbellFileImport(file, time.zone = "UTC", checkduplicates = TRUE,
  skip.rows = NA, importunits = FALSE)
}
\arguments{
\item{file}{filename of the dat (TOA5) file}

\item{time.zone}{Time zone, defaults to "UTC"}

\item{checkduplicates}{Check for and remove duplicate measurements from the file. Logical, defaults to TRUE. A quick pre-check on TIMESTAMP only is done initially. The full deduplication is skipped if no duplicate timestamps exist.}

\item{skip.rows}{Number of rows from the start of the data section to discard. Numeric, defaults to NA.}

\item{importunits}{Import the units row (row 3 of the .dat file) as a separate data frame. Logical, defaults to FALSE. When TRUE, the function returns a named list with elements \code{data} (the measurements data frame) and \code{units} (a single-row data frame of units for each column).}
}
\value{
When \code{importunits = FALSE} (default), a data frame with the imported measurements. When \code{importunits = TRUE}, a named list with two data frames: \code{data} (measurements) and \code{units} (column units).
}
\description{
import a Campbell Scientific data logger *.dat (TOA5) file
}

